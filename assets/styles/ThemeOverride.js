import {createMuiTheme} from "@material-ui/core";
import {lightBlue, orange, red, teal} from "@material-ui/core/colors";

const Customtheme = createMuiTheme({
    palette:{
        primary:teal,
        secondary:orange
    }
})
export default Customtheme;